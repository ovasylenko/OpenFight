edition = "2024"
max_width = 100
hard_tabs = false
tab_spaces = 4
use_small_heuristics = "Default"
reorder_imports = true
reorder_modules = true
use_field_init_shorthand = true
# Keep clippy config in .clippy.toml / clippy.toml — see those files for lints.
